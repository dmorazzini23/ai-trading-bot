## Updated Test Failures - Iteration Required

The agent needs to iteratively resolve ALL failures by running `make test-all` after each fix and debugging until 100% pass rate is achieved.

**Current Failures (3 total):**

### 1. Strategy Allocator Empty Results
```
tests/test_strategy_allocator_smoke.py::test_allocator
assert ([]) - Expected non-empty allocation results with AAPL symbol
```

### 2. Missing min_confidence Attribute (NEW)
```
tests/test_logging_behavior.py::test_cooldown_expired_throttle
AttributeError: 'TradingConfig' object has no attribute 'min_confidence'
strategy_allocator.py:36 -> strategy_allocator.py:59
```

### 3. Bot Engine Import Timeout
```
tests/test_nameerror_integration.py::test_bot_engine_import_no_nameerror
subprocess.TimeoutExpired - Import takes >5 seconds
```

## Critical Requirements:

1. **Replicate the exact testing environment** - Python 3.12.3, same dependencies, same test suite
2. **After EACH code change, run `make test-all`** to verify progress
3. **Debug iteratively** - fix one issue, test, fix next issue, test, repeat
4. **ALL 360 tests must pass** - no stopping at partial fixes
5. **Fix the TradingConfig.min_confidence missing attribute**
6. **Ensure allocator returns proper allocation results**
7. **Optimize bot_engine import to complete within 5 seconds**

The agent must continue the debug-fix-test cycle until achieving 100% test success rate (360 passed, 0 failed).