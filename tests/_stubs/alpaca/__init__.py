"""Minimal stub of alpaca-py package for tests.

This package is intentionally placed under tests/_stubs so it never shadows
the real ``alpaca`` package in production. Tests that need it should add this
directory to ``sys.path`` explicitly or import via relative helpers.
"""

