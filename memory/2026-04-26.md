# 2026-04-26

- `AGENTS.md` is the authoritative operating contract for this repo and must be treated as read-only during memory flushes
- Runtime invariants to preserve: Python 3.12 only, `zoneinfo` only, Flask API on `:9001`, health endpoints on shared `:9001` or standalone `HEALTHCHECK_PORT`, and runtime config access only through `ai_trading.config.management`
- Runtime SDK policy remains pinned to `alpaca-py==0.42.1`; avoid `alpaca-trade-api` in runtime paths
- Repo editing contract requires surgical `apply_patch` edits, no shims, no raw `print` in runtime/tests except stdout assertions, and no repo-local `sudo` write commands
- Validation expectation for runtime/library changes includes `pytest`, `ruff`, `mypy`, `bash scripts/typecheck_strict.sh`, and `python -m py_compile $(git ls-files '*.py')`, with market-hours caution against broad test runs unless approved
