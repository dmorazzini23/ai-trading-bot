from __future__ import annotations

# AI-AGENT-REF: production placeholder for memory optimization hooks

def enable_low_memory_mode() -> None:
    """Placeholder hook for low-memory mode."""
    # Future: downcast dtypes, set read_csv dtype map, etc.
    return None


def report_memory_use() -> dict:
    """Return a minimal, stable structure for tests."""
    return {"rss_mb": None, "notes": "noop"}
